spring.jpa:
  database: mysql
  #hibernate.dialect: org.hibernate.dialect.
  hibernate.ddl-auto: update
  #properties.hibernate.format_sql: true
  show-sql: true

spring.datasource:
  driver-class-name : com.mysql.cj.jdbc.Driver
  url : jdbc:mysql://localhost:3306/sns_db?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
  username : root
  password : 1234

jwt:
  secret-key: donu_study.sns-application-2023.secret_key
  #30days
  token.expired-time-ms: 2592000000